{
  "name": "calculator-suite",
  "version": "1.0.0",
  "description": "E2E suite to validate scientific calculator functionalities",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:report": "lsof -i :9323 2>/dev/null | grep -v COMMAND | awk '{print $2}' | xargs kill -9 2>/dev/null; sleep 1; playwright show-report reports/latest",
    "test:archive-report": "mkdir -p reports/archive && cp -r reports/latest reports/archive/report-$(date +%Y%m%d-%H%M%S)"
  },
  "keywords": ["e2e", "test", "playwright", "calculator"],
  "author": "Darshan",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
